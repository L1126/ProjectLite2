package com.projectlite2.android.model

import androidx.fragment.app.Fragment

class MessageCard(
        val name: String,
        val group: String,
        val time: String,
        val message: String
) {
}